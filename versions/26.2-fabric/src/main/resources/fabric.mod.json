{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description_long}",
  "authors": [
    "Minegasm contributors"
  ],
  "contact": {
    "homepage": "${homepage}",
    "issues": "https://codeberg.org/minegasm/minegasm/issues"
  },
  "license": "AGPL-3.0-only",
  "environment": "client",
  "entrypoints": {
    "client": [
      "net.minegasm.fabric.MinegasmMod"
    ]
  },
  "depends": {
    "fabricloader": ">=0.16.9",
    "fabric-api": ">=${fabricVersion}",
    "minecraft": ">=${minecraftVersion}"
  }
}
